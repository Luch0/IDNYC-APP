# Uncomment the next line to define a global platform for your project
  platform :ios, '9.0'

target 'IDNYC-APP' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for IDNYC-APP
  pod 'GoogleMaps'
  pod 'GooglePlaces'

  target 'IDNYC-APPTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'IDNYC-APPUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
